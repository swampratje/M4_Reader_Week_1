Put your figure files here.

For HTML output, prefer PNG or SVG.

If you currently have PDF figures, consider converting:
- PDF -> SVG (best for line art)
- PDF -> PNG (fine for plots)

Then update the figure filenames in the chapters accordingly.
